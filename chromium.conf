# Default settings for chromium. This file is sourced by /bin/sh from
# the chromium launcher.

# Options to pass to chromium.
CHROMIUM_FLAGS="--ozone-platform-hint=auto --disable-software-rasterizer --disable-dev-shm-usage --no-sandbox --test-type --disable-gpu"
